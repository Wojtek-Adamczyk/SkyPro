public class Enemy
{
    Integer currentHP;
    Integer damage;
    Integer armor;

    public Enemy(Integer currentHP, Integer damage, Integer armor)
    {
        this.currentHP = currentHP;
        this.damage = damage;
        this.armor = armor;
    }
}