public class Follower
{
    Integer currentHP;
    Integer damage;
    Integer armor;

    public Follower(Integer currentHP, Integer damage, Integer armor)
    {
        this.currentHP = currentHP;
        this.damage = damage;
        this.armor = armor;
    }
}